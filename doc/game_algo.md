# 📘 이중 확률 기반 달팽이 경주 설계서

## 1. 필수 환경 변수 및 상수 (Environment Variables)

시스템 작동에 필요한 모든 값을 최상단에 정의합니다.

### 1-1. 월드 설정 (World Settings)
*   **`GOAL_DISTANCE`**: `100.0` (목표 거리, 단위: m)
*   **`DT`**: `0.016` (Delta Time, 60 FPS 기준 1프레임 시간)

### 1-2. 기본 속도 설정 (Base Speed Config)
모든 달팽이의 신체 능력에 대한 설정입니다.
*   **`BASE_SPEED_MEAN`**: `10.0` (평균 기본 속도)
*   **`SPEED_VARIANCE`**: `3.0` (속도 편차 범위)
    *   **의미:** 달팽이는 매 프레임 `7.0` ~ `13.0` 사이의 랜덤한 기본 속도를 가집니다.
    *   **평균 보장:** 랜덤(난수)의 특성상 시간이 흐르면 평균은 `10.0`에 수렴합니다.

### 1-3. 환경 변수 (Seesaw Config)
경기장의 기울기에 대한 설정입니다.
*   **`Seesaw_Value`**: `-1.0` (오르막) ~ `+1.0` (내리막)
    *   초기값: `0.0` (평지)
    *   변화 방식: `-1.0`과 `1.0` 사이를 랜덤하게 오가는 목표값을 향해 부드럽게 변화.
    *   **공정성:** 장기적으로 오르막과 내리막의 확률은 50:50으로 동일.

### 1-4. 캐릭터 설정 (Character Stats)
두 달팽이의 차이는 오직 **'외부 환경에 대한 민감도'** 뿐입니다.
*   **`Sensitivity_A` (도박꾼):** `20.0` (매우 높음)
*   **`Sensitivity_B` (성실맨):** `5.0` (낮음)

---

## 2. 시스템 핵심 로직 (Core Logic)

이 모델의 핵심은 **"변덕스러운 신체 능력(Base Speed)"**과 **"변덕스러운 환경(Seesaw)"**을 합산하여 최종 이동 거리를 결정하는 것입니다.

> **최종 속도 = (가변 기본 속도) + (시소 값 × 민감도)**

### 2-1. 이동 처리 알고리즘 (매 프레임 실행)

```python
# 1. 이번 프레임의 신체 컨디션 결정 (7.0 ~ 13.0 사이 랜덤)
Current_Base_Speed = Random_Range(BASE_SPEED_MEAN - SPEED_VARIANCE, 
                                  BASE_SPEED_MEAN + SPEED_VARIANCE)

# 2. 최종 속도 계산
Final_Velocity = Current_Base_Speed + (Seesaw_Value * Character_Sensitivity)

# 3. 위치 이동
Position += Final_Velocity * DT

# 4. 출발선 후진 제한 (Start Line Clamping)
if Position < 0:
    Position = 0
```

### 2-2. 논리적 근거
1.  **평균의 법칙:** 시소가 평지(0)라면, 두 달팽이는 평균 `10.0`의 속도로 이동하여 약 10초 후에 골인합니다.
2.  **컨디션 난수:** 매 프레임 속도가 `7~13`으로 떨리기 때문에, 시소가 평지라도 두 달팽이는 앞서거니 뒤서거니 하며 엎치락뒤치락합니다. (기계적인 이동 방지)

---

## 3. 캐릭터별 밸런스 및 시뮬레이션 (Character Balance)

### 🐌 A 타입: "인생 한 방" (High Risk, High Return)
컨디션(기본속도)보다 **환경(시소)의 영향**이 압도적으로 큽니다.

*   **민감도:** `20.0`
*   **속도 공식:** `(7~13) + (Seesaw * 20)`
*   **상황별 속도:**
    *   **최악 (오르막 -1.0):** `(7~13) - 20` = **`-13 ~ -7`** (무조건 후진)
    *   **최고 (내리막 +1.0):** `(7~13) + 20` = **`27 ~ 33`** (로켓 가속)
    *   **평지 (0.0):** `7 ~ 13` (B와 비슷하게 경쟁)
*   **특징:** 오르막이 걸리면 뒤로 맹렬히 미끄러집니다. 내리막이 걸리면 순식간에 화면 밖으로 나갈 기세로 달립니다. **등수가 1초마다 바뀔 수 있습니다.**

### 🐌 B 타입: "우직한 전진" (Low Risk, Stability)
환경(시소)의 영향을 적게 받으며, **자신의 컨디션(기본속도)으로 승부**합니다.

*   **민감도:** `5.0`
*   **속도 공식:** `(7~13) + (Seesaw * 5)`
*   **상황별 속도:**
    *   **최악 (오르막 -1.0):** `(7~13) - 5` = **`2 ~ 8`** (느리지만 **절대 전진**)
    *   **최고 (내리막 +1.0):** `(7~13) + 5` = **`12 ~ 18`** (적당히 빠름)
    *   **평지 (0.0):** `7 ~ 13` (A와 비슷하게 경쟁)
*   **특징:** 어떤 악조건(급경사 오르막)에서도 최소 속도 `2.0`을 보장받아 앞으로 갑니다. A가 뒤로 미끄러질 때 묵묵히 추월합니다.

---

## 4. 승패 결정 메커니즘 (Winning Logic)

이 설계에서 승패는 **"후진 페널티를 얼마나 받느냐"**로 결정됩니다.

1.  **A의 패배 패턴:**
    *   오르막에서 속도가 음수(`-`)가 되어 `Position`이 줄어듭니다.
    *   특히 출발선 근처에서 오르막을 만나면, `Position`이 `0` 미만으로 내려가 `0`으로 고정됩니다. 즉, **이동 에너지가 증발(손실)**됩니다.
    *   이 손실이 누적되면 평균 속도 싸움에서 B에게 밀립니다.

2.  **A의 승리 패턴:**
    *   운 좋게 초반에 내리막을 연속으로 만납니다.
    *   후진으로 까먹는 거리 없이, 폭발적인 가속(`속도 30`)만 누적시켜 단숨에 골인합니다.

3.  **B의 승리 패턴:**
    *   A가 오르막에서 뒤로 밀려나며 고생할 때, B는 속도 `2~8`로 꾸준히 거리를 좁히고 역전합니다.

---

## 5. 개발 구현 가이드 (Implementation Summary)

```python
# 초기화
Pos_A, Pos_B = 0.0, 0.0
Seesaw = 0.0

# 게임 루프
while Pos_A < 100 and Pos_B < 100:
    
    # 1. 시소 환경 업데이트 (랜덤 워크)
    Seesaw = Update_Seesaw_Randomly() 

    # 2. A 달팽이 이동
    Base_A = Random(7.0, 13.0)       # 컨디션 난수
    Speed_A = Base_A + (Seesaw * 20) # 민감도 반영
    Pos_A += Speed_A * DT

    # 3. B 달팽이 이동
    Base_B = Random(7.0, 13.0)       # 컨디션 난수
    Speed_B = Base_B + (Seesaw * 5)  # 민감도 반영
    Pos_B += Speed_B * DT

    # 4. 후진 한계선 처리
    if Pos_A < 0: Pos_A = 0
    if Pos_B < 0: Pos_B = 0

    # 5. 렌더링 (화면에 그리기)
    Draw(Pos_A, Pos_B, Seesaw)
```

### ✅ 최종 요약
*   **시간 보너스 삭제:** 순수 물리/확률 싸움.
*   **기본 속도 변동:** `10 ± 3` (생동감 부여).
*   **민감도 차이:** A(20) vs B(5).
*   **재미 요소:** A는 "대박 아니면 쪽박", B는 "꾸준함의 미학".